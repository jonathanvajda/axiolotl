{
  "type": "module",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "eslint . --quiet",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --ci"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "cross-env": "^7.0.3",
    "eslint": "^10.8.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  }
}
